# Ejemplo de configuración de variables de entorno para producción en GCP
# Copia este archivo a .env.production y ajusta los valores según tu configuración

# Configuración del servidor
NODE_ENV=production
PORT=8080

# Configuración de la base de datos
# Para producción, considera usar Cloud SQL en lugar de SQLite
DATABASE_TYPE=sqlite
DATABASE_NAME=/app/data/kredentia.db

# Configuración de JWT
JWT_SECRET=tu-jwt-secret-super-seguro-aqui
JWT_EXPIRES_IN=1h

# Configuración de CORS
CORS_ORIGIN=https://tu-frontend-domain.com

# Configuración de Avalanche/Blockchain
AVALANCHE_RPC_URL=https://api.avax.network/ext/bc/C/rpc
PRIVATE_KEY=tu-private-key-aqui

# Configuración de logging
LOG_LEVEL=info

# Configuración específica de GCP
GOOGLE_CLOUD_PROJECT=tu-project-id
GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json

# Para Cloud SQL (PostgreSQL) - Descomenta si usas Cloud SQL
# DATABASE_TYPE=postgres
# DATABASE_HOST=127.0.0.1
# DATABASE_PORT=5432
# DATABASE_USERNAME=postgres
# DATABASE_PASSWORD=tu-password
# DATABASE_NAME=kredentia
# DATABASE_SSL=true

# Para usar Cloud SQL Proxy
# INSTANCE_CONNECTION_NAME=project:region:instance
